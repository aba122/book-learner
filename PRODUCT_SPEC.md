# PRODUCT_SPEC — book-learner 产品定义

> Mac 本地 EPUB 深度学习软件:西蒙学习法为骨架,费曼学习法为闭环。
> 定位:自用优先,单人,中文界面,快速迭代。

## 1. 产品概述

导入一本 EPUB,app 用 AI 把书拆解成知识块并生成学习地图,用户设定目标与节奏后逐块攻克。每个知识块内完成"阅读原文 → 向 AI 扮演的学生讲授 → 暴露漏洞 → 回读修复 → 通过"的费曼闭环。薄弱点自动回流重考,通过的块进入间隔重复复习。全程强节奏管理,学习成果沉淀为可导出 Obsidian 的档案。

### 学习法 → 功能映射

| 学习法要素 | 产品功能 |
|---|---|
| 西蒙:明确目标 | 导入时设定完成期限/每日块数,生成学习计划 |
| 西蒙:拆解知识 | AI 生成知识地图(可编辑确认),书 → 模块 → 知识块 |
| 西蒙:集中突破 | 单主攻书模式 + 每日队列 + 番茄钟专注计时 |
| 西蒙:持续投入 | 系统通知提醒 + streak + 落后自动重排 |
| 费曼:以教代学 | 对话式学生扮演——AI 扮演好奇初学者听你讲并追问 |
| 费曼:找出漏洞 | 对话中暴露模糊点 + 结束时结构化评估 + 薄弱点清单 |
| 费曼:回补简化 | 一键回读原文锚点 → 再讲 → AI 建议通过/重学 + 用户确认 |
| 遗忘对抗(补充) | 薄弱点次日回流重考 + 1/3/7/14 天间隔复述复习 |

## 2. 用户旅程(完整流程)

1. **导入**:拖入/选择 EPUB → 选择书籍类型(教材/方法论/人文,可 AI 建议)→ AI 分章节分析生成知识地图草稿。
2. **地图确认**:在地图编辑页合并/拆分/删除/跳过/排序知识块,确认后地图定稿(学习中途仍可调整)。
3. **目标设定**:设定完成期限或每日块数(二者换算互显),设定每日提醒时间 → 生成学习计划,该书成为主攻书。
4. **每日学习循环**(核心):打开 app 看到今日队列 → 按序完成:薄弱点重考 → 到期间隔复习 → 新知识块。每个新块内:
   a. 阅读器定位到该块原文范围(高亮标记),阅读;
   b. 进入费曼对话:用文字或语音向 AI 学生讲授,AI 追问,模糊点暴露;
   c. 卡壳时可一键回读原文锚点,回来继续讲;
   d. 结束对话 → AI 给评估卡(评分+薄弱点+建议通过/重学)→ 用户确认通过或重学;
   e. 通过的块进入间隔复习调度,漏洞进入薄弱点清单。
5. **落后处理**:落后时 app 自动重排剩余计划(见 §6),明显落后时建议调整目标。
6. **整书终评**:全部块通过后,AI 出跨章节综合考核(讲出全书框架 + 综合应用题)→ 生成学习报告。
7. **导出**:学习档案一键导出 Markdown 到 Obsidian vault。

## 3. 界面规格

### 3.1 今日学习页(主页)
- 今日队列卡片列表,按序:薄弱点重考(红)→ 到期复习(黄)→ 新知识块(蓝);每卡显示预估时长与队列序号(01/02…),左侧 4px 任务色条是全 app 的视觉签名(三任务色只标数据)。
- 顶部:主攻书信息、总进度环(x/N 块)、连续学习天数 streak、今日已投入时长。
- 番茄钟入口:选中任务后开始专注计时(默认 25+5 分钟,可配置);计时期间菜单栏显示倒计时。页内显示为**工具栏带右侧的倒计时胶囊**(2026-09-20 视觉改版第二批,原右下浮动卡):时间、阶段·任务名、暂停/继续、结束;控制失败在胶囊尾部给警示钮 → 浮层重试。
- 空状态:今日完成后显示鼓励语 + 明日预告;没有排定任务时给「去书架」。

### 3.2 书架页
- 封面网格(按容器宽自适应列数);封面为“首字 + 书脊色条”的合成封面(产品签名);主攻书带强调色描边;其余书显示状态(未开始/已暂停/已完成)。
- 卡片动作(2026-09-20 视觉改版第二批):「阅读」按钮 + 「更多操作」菜单(导出到 Obsidian / 标记为已学完 / 删除),卡片右键同菜单;删除成功有轻提示。
- 已完成书籍仍会产生间隔复习任务(汇入今日队列)。
- 切换主攻书需显式确认(提示当前书计划将暂停)。
- 导入按钮 → 导入向导(§2 步骤 1-3)。
- 删除书(2026-09-09 补):每张卡片可删除,危险确认框说明会一并删除知识地图、学习记录、薄弱点、复习排期、标记与产出,记忆库中该书目录也移除(git 记录一次提交);删除前自动更新当日快照,可从设置页恢复。删主攻书后没有主攻书,由用户另选。导入未完成的书(已落盘未抽取/未生成地图)显示「导入未完成」徽标,同样可删除后重导。

### 3.3 知识地图页
- 按模块分组的知识块列表(每模块一张分组卡,44px 行:序号、标题、依赖、状态标签、评估星级)。
- 编辑模式(2026-09-20 视觉改版第三批):工具栏带「编辑地图」进入,「取消 / 确认定稿」退出;每行悬停或键盘聚焦时出现动作组——上移 / 下移 / 跳过 / 并入上一块 / 拆分 / 删除;编辑后计划自动重算。目标设定为对话框(完成期限 / 提醒时间,Esc = 稍后再定)。
- 块间依赖关系(教材类)以"前置块"标签呈现。

### 3.4 阅读器
- 完整 EPUB 阅读:目录、书签、高亮、进度、字体/字号/行距/主题(亮/暗/纸质)设置。工具栏带(2026-09-20 视觉改版第二批):返回 · 标题 · 目录/书签/标记/阅读设置图标钮 · 放大/收窄 · 收起/展开侧栏;目录与阅读设置是锚定浮层(Esc 关闭,焦点回按钮);右栏是平铺检视列,「学习模式 / 问书 / 脉络图」为分段控件。
- 翻页:两侧圆钮(悬停正文或键盘聚焦时显现)、键盘左右键(焦点在输入框/浮层/菜单里时不翻)、点击正文两侧区域;翻页有轻量滑入过渡(可随系统"减弱动态效果"关闭);设置里可切换单页/双页。点击已有高亮可换色或取消高亮(2026-09-10)。
- 排版目标:微信读书级别——中文字体栈、合理版心宽度、两端对齐、段首缩进可选。
- **学习模式**:从任务卡进入时,自动跳转当前块起始位置,块范围淡色高亮;右侧浮出块信息栏(块名/学习要求/历史状态),底部常驻"开始费曼讲授"按钮。
- 费曼对话中点"回读原文"跳回锚点,读完点"继续讲授"返回对话。
- **问书**(2026-09-16,设计见 `docs/superpowers/specs/2026-09-16-reading-chat-design.md`):右栏与「学习模式」并列的标签(无任务时只有它),ChatGPT 式自由问答;选中文字点「问 AI」把选文带进引用区。对话按书保存、默认续聊、可「另起话题」并回看历史。AI 看到:带入的文字 + 当前章节原文(截断)+ 本话题近 8 轮 + 这本书已提炼的理解状态;只解释、不出题不评估。话题结束/离开阅读器/下次启动时提炼成结构化条目写进 `_reading.md`,反哺费曼追问、评估、复习快问与终评(第二批)。
- **脉络图**(2026-09-19):右栏第三个标签 🗺,与「学习模式」「问书」并列。基于当前阅读进度,让 AI 把"从开头到读到的地方"的内容梳理成一张节点-连线图(自绘,不引第三方库):卡片节点带标题/摘要,连线带关系标签,按逻辑分层。手动点「生成脉络图到当前进度」;每书一张,读得更远时顶部提示"可重新生成到最新进度"。点节点可改名、改摘要、改详情、删节点,改动自动保存(标 userEdited);有手改时「重新生成」先确认。节点按性质配色并标阅读顺序,进度按章节标题显示。与知识地图(导入时全书结构、学习用)独立并存——脉络图是按进度的动态理解梳理。读到更后面时「更新到最新进度」增量补新章、不覆盖手改;「让 AI 按我的理解修正」可针对整图或选中节点;节点浮层里「看原文」跳到对应章节、「问一问这部分」把节点内容带进问书;画布可缩放、方向键选节点。图同步镜像到记忆库 `_lineage.md`(供 codex 上下文)并随 Obsidian 导出为 `02-脉络图.md`。

### 3.5 费曼对话页
- 布局:工具栏带(原文参考开关 · 任务标签 · 标题 · 回读原文 / 结束讲授 / 放弃本次),左侧可隐藏的原文参考栏(开关状态每台设备记住),中部对话流(用户反白墨色气泡、学生卡面气泡),底部输入区(麦克风图标钮 + 文字框 + 发送;语音点按开始、再点结束 → 本地转文字 → 可编辑后发送)。
- AI 学生人设:好奇的初学者,只追问不讲课;回复以打字机动画呈现(系统「减弱动态」时直接出全文)。
- 结束后评估卡为不可关的对话框:准确性/完整性/清晰度星级、薄弱点列表(含"已当场修复"标记)、AI 建议(通过/重学)、[确认通过] [暂不通过,再学一遍] 是唯一出口。
- 教材类通过复述评估后追加"迁移应用"环节(见 §4)。

### 3.6 统计页
- 顶部瓦片(按窗口宽 2/4 列):攻克进度环、连续学习、今日投入、薄弱点收敛。
- 阅读时长(2026-09-21,BL-025):阅读器里"页面可见且 90 秒内有操作"的时间自动累计,按书按日存;统计页显示 总计 / 今天 / 本周(周一起)/ 本月,日(30 天)/ 周(12 周)/ 月(12 月)三档柱状图,以及每本书的累计与最近阅读日。与番茄钟"投入"分开计。
- 进度:各书完成率、预计完成日期 vs 目标日期。
- 投入:每日学习时长/番茄数柱状图、streak 日历热力图。
- 质量:薄弱点新增 vs 修复趋势(带图例)、平均评估星级、复习通过率。
- 每张图表都附读屏数据表(`aria-describedby`),VoiceOver 可逐日读数。

### 3.7 设置页
- 形态(2026-09-20 视觉改版第三批):macOS 系统设置式——内容区左列分区导航(通用 / AI 与导出 / 语音 / 画像 / 数据 / 诊断,点击滚到分区、滚动时跟随),右侧各分区为分组内嵌列表(44px 行);工具栏带右上「保存」;菜单栏 ⌘, 直达。
- 通用:外观(跟随系统 / 浅色 / 深色,即时生效)、番茄钟时长、休息时长、提醒时间、晚间提醒。
- AI 与导出:Obsidian vault 路径;codex 可执行路径(留空自动寻找)。
- 语音:whisper 模型导入/选择/删除、输入设备。
- 画像:知识背景、个人情境(可编辑);已掌握概念、误区模式(AI 积累,只读)。
- 数据:SQLite 快照、登记恢复、记忆库 git 远程与推送。
- 诊断:版本 / 提交 / 构建时间、数据与日志目录、打开日志目录。

### 3.7.1 外观与窗口(视觉改版第一批,2026-09-20)
- 外观默认**跟随系统**;设置页「通用 › 外观」可手动覆盖为浅色/深色。快捷入口(2026-09-21 用户要求,有意偏离 HIG“不做 app 级外观开关”——这是一本书,读者习惯手边有夜灯):侧栏底部日读/夜读钮、菜单「显示 › 外观」、⌥⌘D 互换。阅读器正文主题仍独立(app 深色时正文强制夜读)。
- 窗口用透明标题栏,红绿灯在原生位置(侧栏顶部带的左上;不自定义位置——否则标题栏会吞掉带内点击,BL-026),侧栏为原生毛玻璃材质、内容区不透明;侧栏可隐藏(⌃⌘S / 菜单 / 侧栏右上按钮)。
- 原生菜单栏:攻书(设置… ⌘,)、编辑、显示(隐藏/显示侧栏、全屏)、窗口(⌘W 关窗 = 隐藏)、帮助(打开日志文件夹)。
- 视觉语言:暖纸色 + 宋体标题为签名;赤/琥珀/靛三任务色只标数据(色条、标签、书脊、图表),控件强调色固定为靛蓝(WKWebView 不能可靠取系统强调色);图标为统一的线性图标集;所有文字对比度 ≥ 4.5:1;支持减弱动态、增强对比度、减少透明度。
- 页面骨架(第二、三批):每页顶部一条 52 px 工具栏带(与标题栏一体、可拖动窗口),页级动作放带里;列表/设置用分组内嵌列表;弹出任务用对话框或锚定浮层(Esc 关闭、焦点还原);轻提示不打断。

### 3.8 菜单栏常驻(tray)
- app 关窗后常驻菜单栏:显示今日剩余任务数/番茄倒计时;点击快速恢复窗口;每日提醒由常驻进程发系统通知。

## 4. 三类书籍模板

| | 教材/专业技术书 | 非虚构方法论 | 人文社科/历史 |
|---|---|---|---|
| 学习目标 | 掌握知识并能解决结合现实的新问题 | 掌握观点/框架/案例,产出个人化方法论 | 理解整体脉络,形成自己的思考 |
| 地图拆解方式 | 概念/原理/方法为节点,标注前置依赖 | 观点—框架—案例三层组织 | 叙事阶段/主题脉络组织,弱化定义记忆 |
| 费曼对话侧重 | 概念准确性、原理推导、边界条件 | 观点是否吃透、框架各要素关系、案例还原 | 时间线/因果链是否连贯,AI 追问"为什么会这样发展" |
| 通过后附加环节 | **迁移应用题**:AI 结合用户画像/情境出 1-2 道现实新问题(非书内例题),用户作答,AI 评估思路 | **情境化引导**:AI 引导"结合你当下的情境,这个框架怎么用",输出追加到本书《我的方法论》文档 | **观点讨论**:AI 抛出对立视角或史学争议,用户写下自己的思考,归档为思考笔记 |
| 核心产出物 | 复述终稿 + 应用题解答集 | 复述终稿 + 个人方法论文档 | 脉络图复述 + 思考笔记集 |

## 5. 每日队列与复习规则

- **队列组成与排序**:①薄弱点重考(上限 3 个/日,超出顺延)→ ②到期间隔复习(每个约 5 分钟快速复述)→ ③新知识块(按计划配额)。
- **薄弱点回流**:评估卡中未当场修复的薄弱点,次一学习日插入队列开头,以针对性快问形式重考;连续 2 次通过则标记"已修复"。
- **间隔重复**:块通过当天记为第 0 天,按 1/3/7/14 天调度快速复述(AI 出 1-2 个针对该块的快问,3 分钟内);复习失败 → 生成薄弱点 + 该块间隔重置回 1 天。第 14 天通过后进入"已巩固",不再调度(整书终评仍覆盖)。
- **已完成书**:复习任务照常汇入今日队列,不受主攻书切换影响。

## 6. 强节奏细则

- **目标设定**:输入期限 → 自动算每日块数(扣除复习负担预估);或输入每日块数 → 显示预计完成日。
- **提醒**:每日固定时间系统通知(常驻进程发送);当日队列未完成且到晚间(可配置)追加一次提醒。
- **落后重排**:连续 ≥2 天未完成当日新块配额时触发——剩余块均摊到剩余天数;若均摊结果超过"每日上限"(可配置,默认 4 块),则弹出建议:顺延截止日期或缩减地图(标记跳过低优先级块)。截止日期永不被静默修改,必须用户确认。
- **番茄钟**:任务卡绑定启动,默认 25 分钟专注 + 5 分钟休息;学习时长统计以番茄钟与页面活跃时间合并计算。

## 7. 学习档案与导出

- 每本书自动沉淀:各块复述终稿、评估历史、薄弱点演变、个人方法论(方法论书)、思考笔记(人文书)、应用题解答(教材书)、整书学习报告。
- 一键导出到 Obsidian vault(路径可配置):目录结构与 frontmatter 约定见 TECH_DESIGN §9;支持增量重导(覆盖同名文件)。

## 8. 非目标(明确不做)

- 多用户/账号/云同步服务(备份走本地 git,可选私有远程);
- iOS/iPad 版本;
- DRM 加密 EPUB 的解密;
- V1 不做 AI 语音播报(TTS 仅预留接口);
- 不做社交/分享功能。
