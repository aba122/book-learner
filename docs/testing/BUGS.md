# 缺陷台账(测试阶段,2026-09-09 起)

状态:open → fixing → fixed(PR)→ verified(用户在新包上确认)→ closed;wontfix / duplicate 另注。
每条修复必须带回归用例(用例名含编号)与 DEVLOG 条目;发布批次见 `CHANGELOG.md`。

| 编号 | 级别 | 状态 | 页面 | 现象 | 根因 | 修复 | 回归用例 | 报告日 |
|---|---|---|---|---|---|---|---|---|
| BL-001 | P1 | fixed | 阅读器/费曼 | 「回读原文」只跳到章首、学习模式无块下划线、费曼注入整章原文 | 前端从未调用 `resolveBlockAnchors`/`setAnchorSegments`,原生锚点全为 `chapter_fallback`(CODE_MAP §4) | 导入向导在地图作业后回填锚点(`epub/anchorBlocks.ts`);已导入的书需删除重导才有精确锚点 | `anchorBlocks.test`、library.test「BL-001」 | 09-09(review 发现) |
| BL-002 | P2 | fixed | 地图 | 编辑态只有上下移/跳过/改模块名,没有删除、合并、拆分 | 前端未做 UI;core 只有 merge,delete 没有、split 是占位报错 | PR #41:core `Delete`(只删未学且未进计划/无记录的块,删锚点、清他人 prereq 引用)+ `Split{title_a,title_b}`(原块改名、新块紧随其后、同模块/前置、复制锚点段);编辑态行内「并入上一块 / 拆分 / 删除」,删除与并入可撤销 | core `split_inserts_second_block_*`、`delete_removes_untouched_block_*`;map.test「BL-002」;contract.test delete/split;foundation DTO 形状 | 09-09(review 发现);09-11 修 |
| BL-003 | P3 | fixed | 全局 | 夜读模式刷新/重开后失效 | `store.theme` 只在内存,未持久化 | PR #41:`THEME_KEY`(localStorage)启动读回并打 `data-theme`,切换时写入 | store.test「BL-003」 | 09-09(review 发现);09-11 修 |
| BL-004 | P0 | verified | 导入 | Finder 启动后导入报「AI 暂时没有回应」 | GUI 进程无 Homebrew PATH,codex(node 脚本)127 | PR #32 `ensure_gui_path` | foundation `augmented_path_*` | 09-08(用户) |
| BL-005 | P1 | verified | 书架 | 没有删除书的入口;导入失败的书无法清理 | 功能缺失(ADR-0004 已记) | PR #35 `library_delete_book` + 「导入未完成」徽标 | core `delete_book_*`、foundation `delete_book_*`、library.test ×2 | 09-09(用户) |
| BL-006 | P1 | fixed | 阅读器 | 用户口述(09-09 17:4x):"在阅读器里用鼠标在正文里选中一段文字,松开后,没有小工具条出现" | WKWebView 里 epub.js 的正文 iframe 是 `sandbox="allow-same-origin"`(无脚本),其文档不派发 `selectionchange`,epub.js `selected` 永不触发(调试包实测 0 次) | `EpubView` 每 300 ms 轮询各 contents 的 `getSelection()`,用 `cfiFromRange` 算区间 CFI 上报(同一选区不重复) | reader.test「BL-006」 | 09-09(用户) |
| BL-007 | P2 | fixed | 阅读器 | 用户口述(09-09 18:5x):"高亮之后没有取消高亮的选项" | epub.js 注解 SVG 画在父文档,可收点击;`annotations.highlight` 传入点击回调 | 点击高亮 → 「高亮操作」条:四色换色(`readerMarkUpdate`)、取消高亮(`readerMarkRemove`) | reader.test「BL-007」 | 09-09(用户) |
| BL-008 | P3 | fixed | 阅读器 | 用户口述(09-09 19:0x):"阅读界面可以设置成双页" | 阅读设置固定 `spread: 'none'` | 阅读设置加「双页显示」(prefs.spread → `rendition.spread('auto'/'none')`;开启时正文容器放宽到 80em,epub.js 视口 ≥ 800px 才排双页) | reader.test「BL-008」 | 09-09(用户) |
| BL-009 | P2 | fixed | 阅读器 | 用户口述(09-09 19:0x):"bug:鼠标点击不能左右翻页,只能通过键盘操作" | 同 BL-006:正文 iframe 在 WKWebView 沙箱里收不到鼠标事件,点正文两侧无反应 | 父文档在正文左右各叠 48px 透明翻页区(悬停显渐变),点击即 prev/next;‹ › 按钮与键盘照旧 | reader.test「BL-009」 | 09-09(用户) |
| BL-010 | P3 | fixed | 阅读器 | 用户口述(09-09 19:0x):"翻页可以加上翻书的那种显示特效,而不是干巴巴的翻页";换包 21c2803 后:"我要的是纸质书翻页的那种特效";换包 6dc0831 后:"翻书功能做得不好,退回上个版本先吧";追问后:"翻页的过程太粗糙了,整体就不像翻书" | 无过渡;滑入(PR #40)不算;整页 3D 硬翻(PR #41)不像翻书 | PR #43 纸质卷页:克隆当前正文 iframe 做正面/纸背两层快照,纸角(向后翻右下角、向前翻左下角)被抓起沿折线卷过去、纸背落到另一侧,沿折线两条阴影带;快照就绪后才真正换页,新页在纸下露出;双页只卷正在翻的那页、落到对面页;正在卷时连点忽略;`prefers-reduced-motion` 直接换页。几何 `pageCurl.ts`,DOM 层 `pageCurlOverlay.ts`。小样先给用户过目(artifact「攻书卷页小样」) | pageCurl.test 4、reader.test「BL-010」 | 09-09(用户);09-11 返工;09-11 退回;09-11 卷页 |
| BL-011 | P2 | fixed | 阅读器 | 用户口述(09-11 14:0x):"改成这版后,鼠标要点到页面最左边和最右边才能翻页,之前鼠标点左右半部分就能翻页" | PR #40 的翻页点击区只有两侧各 48 px 透明条;正文 iframe 无脚本,WebKit 不调用挂在它上面的任何 JS 监听器,所以点击不能从 iframe 里收 | PR #43 `pointerLayer.ts`:父文档指针层盖在正文上接管鼠标——单击(位移 ≤ 4 px、按下时无选区、没点到高亮)左半页 prev / 右半页 next;拖动用 `caretRangeFromPoint`+`setBaseAndExtent` 在正文里造选区(高亮工具条照常);双击选词;点到 epub.js 注解 SVG 转发 click;两侧边条去掉 | pointerLayer.test 3、reader.test「BL-009/BL-011」 | 09-11(用户) |
| BL-012 | P1 | fixed | 阅读器 | 用户口述(09-17):“书里的文字不能 copy” | 选区靠 pointerLayer 造在 iframe 里(BL-011),原生 Cmd+C 复制不到、也没有复制入口 | 选区工具条加「复制」按钮 + 阅读器接管 Cmd/Ctrl+C:把当前选区文本 navigator.clipboard.writeText 写入剪贴板 | reader.test「BL-012」 | 09-17(用户) |
| BL-013 | P2 | fixed | 阅读器 | 用户口述(09-17):“夜读模式时书页正文没跟着变,只有边框变” | 侧栏「夜读模式」切 app 的 data-theme=dark(只管外壳);正文 iframe 用独立阅读主题,两者不联动 | ReaderPage:app 夜读时正文强制 night 主题(effectiveTheme = appTheme dark ? night : readerPrefs.theme),日读回落阅读设置主题 | reader.test「BL-013」 | 09-17(用户) |
| BL-014 | P2 | fixed | 阅读器/问书 | 用户口述(09-17):“问书的对话界面太小了,不好看内容” | 问书面板固定 w-72(288px) | 问书 tab 时面板加宽到 w-96,顶部加「放大/收窄」在 w-96 与 w-[40rem](max-w-78vw)间切;消息字号 xs→sm、气泡加内边距 | readingChat.test「BL-014」 | 09-17(用户) |
| BL-015 | P2 | fixed | 阅读器/问书 | 用户口述(09-17):AI 回复出现 **加粗** 原始星号,要更清晰美观 | 第一批用纯文本 whitespace-pre-wrap、未渲染 Markdown | 自写轻量 Markdown 渲染 markdownParse.ts + markdown.tsx(标题/列表/引用/围栏码/粗斜码/链接,React 元素、无 dangerouslySetInnerHTML);AI 气泡走 Markdown、用户气泡仍纯文本 | markdown.test、readingChat.test「BL-015」 | 09-17(用户) |
| BL-016 | P2 | fixed | 书架/阅读器 | 用户反馈:「问书」标签不显眼,且没有纯阅读入口(进阅读器只有今日任务和费曼回读原文两条路) | 标签是小号浅色文字;书架无“读这本书”按钮 | 书架每本 mapped 书加「阅读」→ 用第一个知识块打开阅读器(不带任务),右栏默认「问书」;侧栏标签改为药丸式(选中填色 + 💬)更显眼 | library.test「BL-016」、reader.test 标签 | 09-17(用户反馈) |
| BL-017 | P2 | open | 阅读器/问书 | 用户口述(09-17):打开问书对话框提问,AI 思考时点「收起」对话框,对话就被中断 | 「收起」把 panelOpen 置 false,外层三元把整张 Card(含 ReadingChatPanel)换成收起按钮 → 面板卸载,进行中的 readingSend 乐观气泡/思考中/结果回调随之丢失(回复已写库,但重开是新实例只加载一次,看起来被中断) | 待定:收起改为隐藏不卸载(hidden/display:none),面板常驻,进行中的提问与「思考中」跨收起保留 | 待补 | 09-17(用户) |
| BL-018 | P2 | open | 阅读器 | 用户口述(09-17):阅读时点左上角「返回」,再进来又跳回第一页,而不是上次读到的位置 | 位置写回 800ms 防抖;ReaderPage 卸载的 cleanup 只 clearTimeout 不 flush → 翻页后 800ms 内点返回,最后位置没写库,只剩最初(首页)那次记录。另:从学习任务进阅读器 initialHref 按设计跳到块首段,非自由阅读位置 | 待定:卸载/点返回时 flush 待写位置(立即 readerPositionSet),必要时区分自由阅读与任务起点 | 待补 | 09-17(用户) |
